/**
 * The interfaces are to create a contract that allows you to depend on the skeleton rather than the implementation<br>
 * This allows for better testability since settings don't have to extend {@link com.github.black0nion.blackonionbot.config.discord.api.settings.AbstractSetting AbstractSetting}
 * but can rather be mocked according to the interface
 */
package com.github.black0nion.blackonionbot.config.discord.impl.settings;
